{
  "name": "heroku_app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node backend/index.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build && cd .. && cd backend && npm install"
  },
  "engines": {
    "node": "14.x"
  }
}